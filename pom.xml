<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>org.projectdatahopper.hop</groupId>
  <artifactId>hop-pentaho-reporting</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>Hop Pentaho Reporting</name>
  <description>
    Third-party Apache Hop transform plugin that generates PDF/HTML/Excel/CSV/RTF
    output from Pentaho Reporting (.prpt) files. Distributed under LGPL-2.1 because
    it links against the LGPL Pentaho Reporting classic engine (10.1.x).
    Not an Apache Software Foundation release.
  </description>
  <url>https://github.com/ProjectDataHopper/hop-pentaho-reporting</url>

  <licenses>
    <license>
      <name>GNU Lesser General Public License, Version 2.1</name>
      <url>https://www.gnu.org/licenses/old-licenses/lgpl-2.1.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <modules>
    <module>plugins/pentaho-reporting-output</module>
    <module>assemblies/pentaho-reporting-output</module>
  </modules>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <maven.compiler.release>21</maven.compiler.release>

    <!--
      Hop APIs to compile against. Marketplace install UI needs Hop 2.19+;
      plugin may still run on 2.18.x if APIs match. Override: -Dhop.version=2.19.0-SNAPSHOT
    -->
    <hop.version>2.18.1</hop.version>

    <!--
      LGPL classic engine line (9.4 / 10.0 / 10.1). 10.2+ is BSL — do not use.
      Prefer Jenkins-built 10.1.0.0-SNAPSHOT from pentaho-reporting-lgpl when available;
      otherwise scripts/vendor-reporting-libs.sh → third-party/pentaho-reporting-lib.
    -->
    <!-- Prefer Jenkins-built LGPL 10.1 on Nexus; override if you only have 9.4 vendor jars -->
    <pentaho-reporting.version>10.1.0.0-SNAPSHOT</pentaho-reporting.version>
    <!-- JFreeChart line used by legacy-charts (PRD/PDI classic chart samples) -->
    <jfreechart.version>1.0.13</jfreechart.version>
    <jcommon.version>1.0.16</jcommon.version>
    <!-- Optional flat-folder extras merged into the zip lib/ (may be empty) -->
    <reporting.vendor.lib.dir>${maven.multiModuleProjectDirectory}/third-party/pentaho-reporting-lib</reporting.vendor.lib.dir>

    <!-- Marketplace zip coordinates (deployed to hop-community-plugins) -->
    <marketplace.groupId>org.projectdatahopper.hop</marketplace.groupId>
    <marketplace.artifactId>hop-pentaho-reporting-output</marketplace.artifactId>
    <hop.community.nexus.url>https://repository.data-hopper.com/repository/hop-community-plugins/</hop.community.nexus.url>
    <hop.community.nexus.serverId>hop-community-plugins</hop.community.nexus.serverId>
    <pentaho.reporting.lgpl.nexus.url>https://repository.data-hopper.com/repository/pentaho-reporting-lgpl/</pentaho.reporting.lgpl.nexus.url>

    <lombok.version>1.18.42</lombok.version>
    <jandex.version>3.5.3</jandex.version>
    <junit.version>5.10.2</junit.version>
    <maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
    <maven-jar-plugin.version>3.4.2</maven-jar-plugin.version>
    <maven-dependency-plugin.version>3.8.1</maven-dependency-plugin.version>
    <maven-assembly-plugin.version>3.7.1</maven-assembly-plugin.version>
    <maven-surefire-plugin.version>3.2.5</maven-surefire-plugin.version>
    <maven-deploy-plugin.version>3.1.3</maven-deploy-plugin.version>
  </properties>

  <distributionManagement>
    <repository>
      <id>${hop.community.nexus.serverId}</id>
      <name>Data Hopper community Hop plugins (releases)</name>
      <url>${hop.community.nexus.url}</url>
    </repository>
    <snapshotRepository>
      <id>${hop.community.nexus.serverId}</id>
      <name>Data Hopper community Hop plugins (snapshots)</name>
      <url>${hop.community.nexus.url}</url>
    </snapshotRepository>
  </distributionManagement>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.apache.hop</groupId>
        <artifactId>hop-core</artifactId>
        <version>${hop.version}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>org.apache.hop</groupId>
        <artifactId>hop-engine</artifactId>
        <version>${hop.version}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>org.apache.hop</groupId>
        <artifactId>hop-ui</artifactId>
        <version>${hop.version}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <version>${lombok.version}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>org.projectdatahopper.hop</groupId>
        <artifactId>hop-transform-pentaho-reporting-output</artifactId>
        <version>${project.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <defaultGoal>package</defaultGoal>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${maven-compiler-plugin.version}</version>
          <configuration>
            <release>${maven.compiler.release}</release>
            <annotationProcessorPaths>
              <path>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
              </path>
            </annotationProcessorPaths>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-jar-plugin</artifactId>
          <version>${maven-jar-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-dependency-plugin</artifactId>
          <version>${maven-dependency-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-assembly-plugin</artifactId>
          <version>${maven-assembly-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${maven-surefire-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>io.smallrye</groupId>
          <artifactId>jandex-maven-plugin</artifactId>
          <version>${jandex.version}</version>
          <executions>
            <execution>
              <id>make-index</id>
              <goals>
                <goal>jandex</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

  <repositories>
    <repository>
      <id>central</id>
      <url>https://repo.maven.apache.org/maven2</url>
      <releases>
        <enabled>true</enabled>
      </releases>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </repository>
    <repository>
      <id>apache.snapshots</id>
      <url>https://repository.apache.org/snapshots/</url>
      <releases>
        <enabled>false</enabled>
      </releases>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
    </repository>
    <repository>
      <id>pentaho-reporting-lgpl</id>
      <name>Data Hopper LGPL reporting engine</name>
      <url>${pentaho.reporting.lgpl.nexus.url}</url>
      <releases>
        <enabled>true</enabled>
      </releases>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
    </repository>
  </repositories>
</project>
